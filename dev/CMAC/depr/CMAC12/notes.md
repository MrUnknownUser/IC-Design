#paper
## ASG usage
- good initializsation needed (seed the LSFRs)
- with long random bits
- otherwise we get randomness after around 2500ns of initialisation (seeded R1 with 11011 and R2 with 11011)
- longer seeds (close to LSFR size) give randomness as soon as we need it
